const request = require("supertest");

describe('TicTacToe e2e test', () => {
    describe('Given the /TicTacToe/newGame', () => {
        test('return the game played in BOT mode to the user', () => {
            
        });
        
    });
});